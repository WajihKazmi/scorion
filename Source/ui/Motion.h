#pragma once

/** Shared motion timing (SCORION_UI_MASTER_SPEC). */
namespace Motion
{
    inline constexpr int hoverMs   = 120;
    inline constexpr int clickMs   = 90;
    inline constexpr int panelMs   = 300;
    inline constexpr int bgCycleMs = 20000;
    inline constexpr float hoverSec = 0.120f;
    inline constexpr float panelSec = 0.300f;
}
